The dataset and code should be used in the following way:

Use modelling_code.R to import dataset_alldata.csv and run GLMMs. Output model_output.rda.

Use visualisation_code.R to import dataset_alldata.csv and model_output.rda to plot figures
and requires vcm.stan to run bayesian hierarchical model.